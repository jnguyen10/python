#Python - Assignment #4 - Odd/Even

for index in range(1,2001):
	if (index % 2) != 0:
		print "Number is %s.  This is an odd number." % index
	else:
		print "Number is %s.  This is an even number." % index
